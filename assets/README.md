# Reference figures

These images visualize the reference single-run benchmark and were not generated under the
current provenance contract. In particular, `representative_predictions.png` contains the
known whole-image overlay tinting defect. New evaluation runs create a corrected
foreground-only overlay inside their own output directory.
