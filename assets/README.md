# Legacy figures

These images visualize the earlier single-run baseline and were not regenerated by the
repaired pipeline. In particular, `representative_predictions.png` contains the known legacy
overlay tinting defect. New evaluation runs create a corrected foreground-only overlay inside
their own output directory.
